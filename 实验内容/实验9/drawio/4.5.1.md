```mermaid
graph TD
    %% 客户端层
    subgraph 客户端终端 [客户端终端]
        W[Web端 WebView / Vue3]
        M[移动端 Mobile / Flutter]
        MP[小程序 Mini-program / uni-app]
    end

    %% API 接入层
    subgraph TradeX_后端服务 [TradeX 后端服务]
        API[REST API 网关 - Django / 端口:7001]
        WS[WebSocket - Daphne / 端口:7002]

        %% 核心业务模块
        subgraph 业务逻辑层 [业务逻辑核心模块]
            M_User[用户与认证模块<br/>auth/users]
            M_Prod[商品与分类模块<br/>products/categories]
            M_Order[订单全流程模块<br/>orders]
            M_Forum[论坛模块<br/>forum]
            M_Chat[实时聊天模块<br/>chat]
            M_RAG[AI 导购模块<br/>rag]
        end
        
        API --> M_User
        API --> M_Prod
        API --> M_Order
        API --> M_Forum
        API --> M_RAG
        
        API -->|历史记录/REST| M_Chat
        WS -->|实时通讯/ws| M_Chat
    end

    %% 数据与外部依赖层
    subgraph 数据与外部依赖 [数据与基础设施层]
        DB[(核心业务数据库<br/>SQLite)]
        VDB[(向量数据库<br/>ChromaDB)]
        COS[对象存储<br/>腾讯云 COS]
        LLM[大模型 API<br/>OpenRouter LLM]
    end

    %% 终端到网关的调用连线
    W -->|HTTP GET/POST/REST /api/*| API
    M -->|HTTP GET/POST/REST /api/*| API
    MP -->|HTTP GET/POST/REST /api/*| API

    W -->|WebSocket ws://.../ws/chat/| WS
    M -->|WebSocket ws://.../ws/chat/| WS

    %% 模块到数据的连线
    M_User -->|读写| DB
    M_Prod -->|读写| DB
    M_Order -->|读写| DB
    M_Forum -->|读写| DB
    M_Chat -->|读写| DB
    
    M_Prod -->|上传图片/文件| COS
    M_RAG -->|检索商品向量| VDB
    M_RAG -->|SSE 流式对话| LLM